{% extends "::base.html.twig" %}

{% block title %}LJTrToolRegistrationBundle:Condition:new{% endblock %}

{% block body %}

    <h1>Welcome to the Condition:new page</h1>

    {{ form_start(form, { 'attr': {'class': 'form-horizontal '}}) }}
        <fieldset>
            <div class="form-group">
                {{ form_label(form.displayName, null, {'label_attr': {'class':'control-label col-sm-2'}}) }}
                <div class="col-sm-10">
                    {{ form_widget(form.displayName, {'attr': {'class': 'form-control', 'placeholder': 'Anforderung'}}) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(form.conditionName, null, {'label_attr': {'class':'control-label col-sm-2'}}) }}
                <div class="col-sm-10">
                    {{ form_widget(form.conditionName, {'attr': {'class': 'form-control', 'placeholder': 'interner Name'}}) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(form.description, null, {'label_attr': {'class':'control-label col-sm-2'}}) }}
                <div class="col-sm-10">
                    {{ form_widget(form.description, {'attr': {'class': 'form-control', 'placeholder': 'Beschreibung'}}) }}
                </div>
            </div>
        </fieldset>
        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                {{ form_widget(form.Anlegen, {'attr': {'class': 'btn btn-default'}}) }}
            </div>
        </div>
    {{ form_end(form) }}
{% endblock %}